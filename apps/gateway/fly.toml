app = "nargis-gateway"

[build]
  image = ""

[env]
  # Set via fly secrets or CI
  STT_URL = ""
  LLM_URL = ""

[[services]]
  internal_port = 8080
  protocol = "tcp"
  [[services.ports]]
    port = 443
    handlers = ["http"]
  [[services.ports]]
    port = 80
